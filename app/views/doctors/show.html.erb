<div id="page-content">
    <h1 class="text-center mt-4"><%= @doctor.firstName + ' ' + @doctor.lastName%>'s profile</h1>
    <div class="text-center mt-4">    
        <% if logged_in? && @doctor==current_doctor %>
        <%=link_to "Edit your profile", edit_doctor_path(@doctor),class: "btn btn-info" %>
        <% end %>
    </div>
<div class="apple_pagination">
    <%= will_paginate @patients, :container => false %>
  </div>
    <h3 class="text-center mt-4">Patients</h3>
    <%= render 'patients/patient' %>
</div>
<div class="apple_pagination mb-4 mt-4">
    <%= will_paginate @patients, :container => false %>
</div>